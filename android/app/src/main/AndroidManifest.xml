<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Hardware feature declarations for Play Store device filtering -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- Android TV support (required="false" = same APK for phone + TV) -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <!-- Bluetooth is optional — used for game controllers, not required for core functionality -->
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <!-- Bluetooth controller support (Steam Controller BLE, HID devices) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Haptic feedback for game controllers -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:hasFragileUserData="true"
        android:requestLegacyExternalStorage="true">

        <!-- Native Compose UI (launcher) -->
        <activity android:name="com.blitterstudio.amiberry.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Amiberry.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Android TV launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!--
                Open Amiga disk images, configs, and WHDLoad archives from file managers.
                Split into two filters: file:// uses pathPattern matching; content:// uses
                application/octet-stream since pathPattern is unreliable with SAF URIs.
                Non-Amiga files are rejected in importAndLaunch() via extension check.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.adf" />
                <data android:pathPattern=".*\\.adz" />
                <data android:pathPattern=".*\\.dms" />
                <data android:pathPattern=".*\\.ipf" />
                <data android:pathPattern=".*\\.hdf" />
                <data android:pathPattern=".*\\.iso" />
                <data android:pathPattern=".*\\.cue" />
                <data android:pathPattern=".*\\.chd" />
                <data android:pathPattern=".*\\.lha" />
                <data android:pathPattern=".*\\.lzx" />
                <data android:pathPattern=".*\\.lzh" />
                <data android:pathPattern=".*\\.uae" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
        </activity>

        <!-- FileProvider for sharing config files via the system share sheet -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- SDL emulator activity (launched by native UI) -->
        <activity android:name="com.blitterstudio.amiberry.AmiberryActivity"
            android:process=":sdl"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode|navigation"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:enableOnBackInvokedCallback="true"
            android:exported="false">
            <meta-data android:name="SDL_MAIN_LIBRARY_NAMES" android:value="amiberry" />
        </activity>
    </application>

</manifest>
